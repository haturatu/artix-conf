$ zgrep ZSWAP /proc/config.gz
$vim /etc/default/grub

GRUB_CMDLINE_LINUX_DEFAULT='... zswap.enabled=1 zswap.compressor=zstd zswap.max_pool_percent=10 zswap.zpool=z3fold'

$ sudo grub-mkconfig -o /boot/grub/grub.cfg

---------------------------------------------------------------------------

modprobe options

ThinkPad
$ printf 'options thinkpad_acpi fan_control=1\n' | sudo tee /etc/modprobe.d/thinkpad_acpi.conf

Nvidia GPU
$ sort /proc/driver/nvidia/params | grep -i resizable
$ printf 'options nvidia NVreg_EnableResizableBar=1\n' | sudo tee /etc/modprobe.d/nvidia-rebar.conf
$ sort /proc/driver/nvidia/params | grep -i resizable

$ printf 'blacklist noveau\noptions nouveau modeset=0\n' | sudo tee /etc/modprobe.d/blacklist-nouveau.conf

rebuild initramfs
$ sudo mkinitcpio -P

.
├── README
└── etc
    ├── docker
    │   └── daemon.json
    ├── logrotate.conf
    ├── logrotate.d
    │   ├── chrony
    │   ├── cups
    │   ├── docker
    │   ├── nginx
    │   ├── openrc
    │   ├── pacman
    │   └── postgresql
    ├── pacman.conf
    ├── pacman.d
    │   ├── mirrorlist
    │   └── mirrorlist-arch
    ├── sysctl.d
    │   ├── 93_ipv6_neighbor_cache.conf
    │   ├── 94_tcp_mtu_probing.conf
    │   ├── 95_dirty_ratio.conf
    │   ├── 96_swappiness.conf
    │   ├── 97_disable_watchdog.conf
    │   ├── 97_magic_sysrq.conf
    │   ├── 98_http_opt.conf
    │   └── 99_bbr.conf
    └── thinkfan.conf

6 directories, 22 files
